import { SxProps } from '@mui/material';

interface ToneCreatorStyles {
  container: SxProps;
  title: SxProps;
}

export const toneCreatorStyles: ToneCreatorStyles = {
  container: {
    width: '100%',
    p: 4,
  },
  title: {
    mb: 4,
  },
}; 